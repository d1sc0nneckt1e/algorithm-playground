import React, { useRef, useState } from "react";
import { IonApp } from "@ionic/react";

const AlgorithmOverview: React.FC = () => {
  return (
    <IonApp>
      <h2>AlgorithmOverview</h2>
    </IonApp>
  );
};

export default AlgorithmOverview;
